# Archive

Completed and superseded working documents. **Nothing here is operative.**
None of it is read during a session, and no rule in it binds — the live rule
set is `CLAUDE.md`, and the live strategy is
`docs/superpowers/specs/2026-08-13-competition-strategy-design.md`.

These are kept as provenance: they record why things were decided, which is not
reconstructable from the current documents.

| Document | What it is |
|---|---|
| `2026-08-14-day-one-candidates.md` | The core-sleeve screen run the evening before the window opened. Candidates, not decisions. |
| `2026-08-14-close-review.md` | Review of the first session's close. |
| `2026-08-14-deep-research-system.md` | Implementation plan for the deep-research loop. Fully executed; the loop it describes is live. |
| `2026-08-14-screener-api-spike.md` | Feasibility spike on third-party screener APIs. Concluded `no-API`. |
| `schwab-mcp-auto-approve-stops.md` | Patch note for the broker MCP's stop-order approval path. |

**File paths quoted inside these documents describe the repository layout as it
was when they were written**, before this archive existed and before the trade
log, status output, and research output were untracked. They are left unedited
on purpose — correcting them would defeat the point of keeping them.
