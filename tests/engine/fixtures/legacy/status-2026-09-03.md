# 2026-09-03 — session close

Superseded block retained for history — this heading must NOT be read by the
parser (it looks like the current block but sits before it in the file).

### State recorded — superseded

- Account value: **$3,700.00** (stale, pre-close read)
- High-water mark: $3,750.00 — stale value, must be ignored

### State recorded — current

- Account value: **$3,728.71** (`liquidationValue`, `currentBalances`, live read ~19:15 ET)
- Competition capital: **$2,828.71** (legacy serialization key only — the
  reserve-adjusted number CLAUDE.md's header says not to compute a second time)
- **High-water mark: $3,800.00 — carried unchanged.** Prior mark resolved via
  `scripts/latest-status.sh --hwm` against the prior session's file.
- Halt threshold **$3,040.00** (0.80 x $3,800.00 HWM) . drawdown **-1.88%**
  against the high-water mark above.
- Positions (2), all `settledLongQuantity` = `longQuantity`:
  - **AMH** 29 sh @ $34.79 avg (mkt $948.59, day P/L -$11.60) — stop 32.01/30.40 GTC WORKING (orderId 1000000000001), qty 29 — matches position qty
  - **CSX** 4 sh @ $50.38 avg (mkt $201.52, day P/L -$2.10) — stop 45.20/42.93 GTC WORKING (orderId 1000000000002), qty 4 — matches position qty

### Watch table

Nothing here should be parsed by the shadow-diff tool; the block ends at the
heading above.
